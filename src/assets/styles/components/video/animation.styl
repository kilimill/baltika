.video
	&__marquee
		opacity 0
		transition opacity .8s
	&__frame
		opacity 0
		transition opacity .6s

.video[loaded]
	.video__frame
		opacity 1
		// transition-delay .45s
	.video__marquee
		opacity 1
		// transition-delay .2s